 hosts: localhost
  become: yes
  gather_facts: no
  tasks:
  - name: starting mahesh java docker
    docker_container:
      name: leela-java
      image: java
      state: started
      recreate: yes
      pull: yes
      restart: yes
      hostname: localhost
      restart_policy: unless-stopped
      ports:
         - "555"


